# Leave blank when the frontend and FastAPI backend share one domain.
# For a separately deployed frontend, use the public backend origin only:
# VITE_API_BASE_URL=https://your-api.onrender.com
VITE_API_BASE_URL=
